# Auto-Selp Project Context

## 1. 실행 및 테스트 (Execution & Tests)
- **로컬 개발 환경 시작**: `./run.sh dev`
  - Docker Compose(`docker-compose.local.yml`)를 사용하여 백엔드, 프론트엔드, DB, Redis를 통합 실행합니다.
  - 백엔드는 8000번 포트에서 작동하며 Hot Reload가 활성화됩니다.
- **테스트 실행**: `./run.sh test`
  - 전체 인프라를 띄우고 `pytest`를 실행한 후 자동으로 리소스를 정리합니다.
- **환경 정리**: `./run.sh down`
  - 모든 컨테이너와 빌드된 이미지를 삭제합니다.

## 2. 데이터베이스 (Database)
- `./run.sh dev` 실행 시 도커 환경 내에서 `DATABASE_URL`이 설정됩니다.
- 로컬 개발 시 DB 접근이 필요하면 컨테이너가 실행 중인 상태에서 연결해야 합니다.

## 3. 리팩토링 규칙 (Refactoring Rules)
- **보안**: 비밀번호는 `Argon2` 알고리즘으로 해싱되어야 합니다. (`src/api/auth_utils.py` 참고)
- **아키텍처**: 대규모 데이터 처리는 `ExcelProcessingService`를 통해 `ProcessingPipeline`과 `BaseProcessorStage` 기반으로 수행합니다.
- **상태 관리**: 진행률 및 상태 업데이트는 서비스 레이어에서 전담합니다.
